# ioTest
> Java_Introduction ioTest
+ CapitalApp2_파일 형태로 나라와 수도 출력하기 (파일: myCapitat.txt)
+ DataStreamTest_DataInputStream과 DataOutputStream 설명
+ FileExam_파일과 디렉토리 설명
+ FileInputOutputTest_input.txt 파일을 읽어서 라인 앞에 번호를 붙여 출력하기 (파일: input.txt, copy2.txt)
+ FileInputStreamTest1_input.txt파일을 읽어서 콘솔창에 출력하기
+ FileInputTest_InputStreamTest에서 내보낸 파일을 읽어서 콘솔창에 내보내기
+ FileTest_Synchronized된 자바 파일을 읽어들이기
+ Friend, FriendMain_친구를 등록하고 파일로 출력하여 입력된 친구의 정보를 볼 수 있도록 만들기 (파일: myFriend.txt)
+ InputStreamTest_파일로 내보내는 객체를 만들어서 키보드로부터 받은 값을 파일로 내보내기
+ InputTest, InputTest01_입력받은 값을 텍스트 파일로 내보내고, 파일에서 값을 읽어서 출력하기
+ TokenFileTest_input.txt라는 텍스트 파일을 만들어서 같은 패키지 안에 저장하고 콘솔창에 출력하기 & 파일로 출력하기 (파일: out.txt)
+ TokenFileTest2_파일의 내용을 한 줄씩 읽어들이고 한 줄씩 출력하기 (파일: input.txt, hex.txt)
+ TokenFileTest3_TokenFileTest2에서 split대신에 StringTokenizer 클래스를 이용하기 (파일: input.txt, octal.txt)